let n = prompt("Enter number of element in the Array: ")
n = Number.parseInt(n)
let Arr =[];
for(let i=0;i<n.length;i++){
    let inp = prompt("Enter Elements: ")
    Arr.push[inp]
}
console.log(Arr)